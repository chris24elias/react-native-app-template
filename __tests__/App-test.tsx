/**
 * @format
 */

import 'react-native';
// Note: test renderer must be required after react-native.
import renderer from 'react-test-renderer';

// wont work need to mock native API's
it('renders correctly', () => {
  //   renderer.create(<App />);
});
